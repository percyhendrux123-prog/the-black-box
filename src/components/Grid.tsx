/**
 * Hairline vertical+horizontal grid, radial-masked.
 * Opacity is 0 in Sohne mode and fades to 1 in Terminal mode (CSS-driven).
 */
export default function Grid() {
  return <div className="tbb-grid" aria-hidden />;
}
