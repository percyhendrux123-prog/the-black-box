/**
 * The whisper marquee — "THE BLACK BOX" set huge and nearly invisible
 * behind the stage. Mix-blend screen so it only catches highlights.
 */
export default function Marquee() {
  return <div className="tbb-marquee" aria-hidden>THE BLACK BOX</div>;
}
