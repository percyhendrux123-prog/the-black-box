/**
 * SVG noise overlay at 4.5% opacity (6% in Terminal mode).
 * Inline data URL so there's no extra HTTP request and it survives offline.
 */
export default function Grain() {
  return <div className="tbb-grain" aria-hidden />;
}
