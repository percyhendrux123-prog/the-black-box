/**
 * Smooth radial gradient + two elliptical spotlights + warm rim.
 * Lighting shifts via parent's data-mode attribute.
 */
export default function Lights() {
  return <div className="tbb-lights" aria-hidden />;
}
